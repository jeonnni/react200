import { Component } from "react";

/**
 * component 란 특정 코드 뭉치를 다른 부분에 이식하거나 재사용하기 위해 사용하는 코드 블록 단위
 * return 된 html 코드를 render() 함수를 사용해 화면에 표시한다
 */
class R003ImportConponent extends Component {
    render() {
        return (
            <h1>[THIS IS IMPORTED COMPONENT]</h1>
        )
    }
}

export default R003ImportConponent;